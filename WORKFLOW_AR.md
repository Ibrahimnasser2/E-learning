# شرح مشروع MANAMU — سير العمل الصحيح

## نظرة عامة

منصة تعليم إلكتروني بتقنية **RAG**: الدكتور يرفع PDF، والطالب يسأل المساعد ويجيب **من مواد مقرراته فقط**.

**My Courses** = منصة مستقلة لإنشاء وإدارة المقررات (مع تحديد المستوى) — ليست خطوة الربط الرئيسية.

---

## الأدوار

| الدور | المهمة |
|--------|--------|
| **إداري** | رفع Excel لإنشاء حسابات الطلاب والدكاترة فقط |
| **دكتور** | إنشاء مقررات (في My Courses) → رفع طلاب (ID + مستوى + مقررات) → رفع PDF |
| **طالب** | اختيار مقرره → سؤال المساعد |

---

## سير العمل

### 1) الإداري — إنشاء الحسابات
يرفع ملفات Excel للطلاب والدكاترة → يتفتحلهم حسابات.

### 2) الدكتور — My Courses (منصة مستقلة)
ينشئ المقررات ويحدد **Level** لكل مقرر.  
كل مقرر له **ID** يظهر على البطاقة (يُستخدم في Excel الطلاب).

### 3) الدكتور — Sidebar → Upload Students
يرفع Excel بالأعمدة:

| الرقم الجامعي | المستوى | المقررات |
|---------------|---------|----------|
| 441234567 | 1 | 1,2 |
| 441234568 | 2 | 3 |

- الطلاب لازم يكونوا موجودين من رفع الإداري
- النظام يربط كل طالب بمقرراته ومستواه
- API: `POST /faculty/upload-students`

### 4) الدكتور — Sidebar → Upload Materials
1. يختار المستهدف: **طلاب** و/أو **أعضاء هيئة التدريس** (أو كلاهما)
2. لو طلاب: يختار **المستوى** → تظهر مقررات هذا المستوى → يختار **مقرر أو أكثر**
3. يرفع الـ PDF

### 5) الطالب
يختار المقرر من الشات → يسأل المساعد → الإجابة من PDFs هذا المقرر فقط.

---

## العزل الصارم (لا أحد يرى بيانات غيره)

| القاعدة | التطبيق |
|---------|---------|
| طالب ↔ طالب | لا يرى مواد مقررات مش مسجّل فيها |
| مستوى ↔ مستوى | مقررات المستوى 1 منفصلة عن المستوى 2 |
| طالب جديد | لا يرى PDF رُفع قبل تاريخ تسجيله (`upload_time >= enrolled_at`) |
| دكتور ↔ دكتور | كل دكتور يرى ويُفهرس **ملفاته هو فقط** |
| المساعد الذكي | يفلتر المصادر بـ `allowed_sources` — قائمة فارغة = لا إجابة من وثائق |

يُطبَّق في: قائمة الملفات · التحميل · الفهرسة · RAG في الشات.

---

## ملخص اختبار سريع

1. إداري → ارفع طلاب + دكاترة  
2. دكتور → My Courses → أنشئ مقرر بمستوى (احفظ الـ ID)  
3. دكتور → Upload Students → Excel (ID, مستوى, IDs المقررات)  
4. دكتور → Upload Materials → مستوى → مقررات → PDF  
5. طالب → اختر المقرر → اسأل  

---

*آخر تحديث: أغسطس 2026*
